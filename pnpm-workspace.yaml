# pnpm の設定ファイル（新しい pnpm では package.json の "pnpm" フィールドではなくこちらが正準）。
# モノレポ構成のワークスペース定義。アプリは apps/*、共有パッケージは packages/* に配置する。
packages:
  - 'apps/*'
  - 'packages/*'

# ビルドスクリプトを持つ依存を明示的に承認し、install 時の ERR_PNPM_IGNORED_BUILDS を防ぐ。
onlyBuiltDependencies:
  - sharp
  - unrs-resolver
